Question:
If you create a constructor method to build a DVD by title then create a constructor method to build a DVD by category. Does JAVA allow you to do this?

Answer:
No. Java does not allow this because both constructors would have the same signature, DigitalVideoDisc(String). Constructor overloading is based on the parameter list, not on the variable names.