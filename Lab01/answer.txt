Q1. What happens if users choose "Cancel"?
- The mini window will close.
- The program won't  run.
- The code won't be saved.
- Next tome it will still ssk for your choice.

Q2. How to customize the options to users?
- Use JOptionPane :

import javax.swing.JOptionPane;

public class ChoosingOption {
    public static void main(String[] args) {
        Object[] options = {"I do", "I don't"};

        int option = JOptionPane.showOptionDialog(
                null,
                "Do you want to continue?",
                "Custom Option",
                JOptionPane.DEFAULT_OPTION,
                JOptionPane.QUESTION_MESSAGE,
                null,
                options,
                options[0]
        );

        String result;

        if (option == 0) {
            result = "I do";
        } else if (option == 1) {
            result = "I don't";
        } else {
            result = "Closed";
        }

        JOptionPane.showMessageDialog(null, "You've chosen: " + result);
        System.exit(0);
    }
}